<UserControl x:Class="FileCopyUtility.Controls.ModernProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <!-- Background -->
        <Border Background="#E5E7EB" 
                Height="16" 
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="#D1D5DB">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="4" ShadowDepth="1"/>
            </Border.Effect>
        </Border>
        
        <!-- Progress Fill -->
        <Border x:Name="ProgressFill"
                Background="#7B1FA2" 
                Height="14" 
                CornerRadius="7"
                HorizontalAlignment="Left"
                Margin="1"
                Width="0">
            <Border.Effect>
                <DropShadowEffect Color="#7B1FA2" Opacity="0.3" BlurRadius="6" ShadowDepth="0"/>
            </Border.Effect>
        </Border>
        
        <!-- Speed Indicator Overlay -->
        <Border x:Name="SpeedIndicator"
                Background="#4CAF50" 
                Height="4" 
                CornerRadius="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="1,2,1,0"
                Width="0"
                Opacity="0.7"/>
        
        <!-- Progress Text -->
        <TextBlock x:Name="ProgressText"
                   Text="0%"
                   FontSize="11"
                   FontWeight="Medium"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>